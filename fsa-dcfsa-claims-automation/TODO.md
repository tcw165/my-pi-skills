P0:
- Script-ify the scoring for deterministic scoring flow
    - input: shell doc for getting rules, shell doc for scoring?
    - output: direct write to DB
    - Debug the scoring script

P1:
- Ingest-cli also take stdin pipe to reduce the output token from writing scripts in flight

